; ~/.gitconfig.local should have 'user.name' and 'user.email'
[include]
	path = ~/.gitconfig.local
[pager]
	branch = false
[core]
	pager = diff-so-fancy | less --tabs=4 -R
[color]
	ui = true
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22
[color "diff"]
	meta = cyan
	frag = magenta bold
	commit = yellow bold
	old = red bold
	new = green bold
	whitespace = red reverse
[diff-so-fancy]
	markEmptyLines = false
